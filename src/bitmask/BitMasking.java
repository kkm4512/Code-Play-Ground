package bitmask;

public class BitMasking {
    public void bitMaskingCalculate(String[] s) {
        int n = s.length;
        // n이 10이라면 1024의 모든 경우의 수를 순회함
        for ( int i=0; i<(1 << n); i++ ) {

        }
    }
}
